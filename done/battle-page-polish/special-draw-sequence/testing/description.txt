Write unit tests for the timeline construction pipeline. Verify that a last-card draw produces the expected ordered step list for each special type, that spdSpecial recursion flattens correctly, that shuffle-back steps contain the right cards, and that the timeline integrates with the existing actionPlays/loading pipeline without breaking existing tests.

## Implementation

### 1. Test infrastructure

Add a new test class `SpecialTimelineTests` in `MMM/tests.py` (or a new `MMM/test_special_timeline.py` — prefer adding to existing tests.py for simplicity).

Helper methods:
```python
def _create_game_with_specific_stats(self, int_count, spd_count, vis_count, res_count):
    """Create a game where the human participant has the given face-up lane card counts.
    This ensures a specific special type triggers."""
    # Build decks so that after drawing down to empty, the stats match
    # The starting card is played to a lane automatically
    # Add more face-up cards to specific lanes to control stats
    ...
```

### 2. Test: no special on non-empty deck

When a draw does NOT empty the deck, no timeline should be produced:
- Draw a card when there are still cards in the deck
- Assert response context does NOT contain timelineSteps (or it's empty)
- Assert specialActions() was NOT called

### 3. Test: intSpecial timeline structure

Draw the last card with Intelligence as the highest stat:
- Set up board so intCount dominates
- POST draw on last card
- Assert timeline has at least 2 steps: [special-trigger, card-effect, shuffle-back]
- Step 0: kind=special-trigger, banner contains "enacts master plan", lane=1
- Step 1: kind=card-effect, affectedCards list matches the hand cards that were played
- Last step: kind=shuffle-back, affectedCards count matches untrusted face-up + hand cards
- Verify banner text matches exact sentence from views.py

### 4. Test: resSpecial timeline structure

Draw the last card with Resolve as the highest stat:
- Set up board so resCount dominates
- Assert timeline trigger step has lane=4, banner contains "holds and trusts"
- Assert card-effect steps include trust flags on affected cards
- Assert affected cards match those that received trust

### 5. Test: visSpecial timeline (win)

Draw the last card with Viciousness highest and opponentRes < visCount:
- Step 0: special-trigger, lane=3, banner contains "attacks and defeats"
- Step 1: card-effect (trusted resolve card glows)
- Step 2: participant-effect (opponent defeated, dimmed)
- Last step: shuffle-back

### 6. Test: visSpecial timeline (drain)

Draw the last card with Viciousness highest but NO trustable resolve card:
- Step 0: special-trigger, banner contains "drains the last of their resolve"
- Step 1: participant-effect (self defeated)
- No card-effect step (no resolve card to glow)
- Last step: shuffle-back

### 7. Test: spdSpecial timeline (rush down + flattening)

Draw the last card with Speed highest and power > opponentPower:
- Step 0: special-trigger (rush down banner), lane=2
- Step 1..N: opponent's special(IntSpecial/ResSpecial) steps appended flat
- Verify the opponent's steps appear in the SAME timeline at the top level
- Verify opponent's trigger steps have the opponent's participantId
- Verify there are NO nested timeline objects

### 8. Test: spdSpecial timeline (flee)

Draw the last card with Speed highest, opponentPower >= power, and speed > opponentSpd:
- Step 0: special-trigger (flee banner), lane=2
- Step 1: participant-effect (fled participant)
- Last step: shuffle-back

### 9. Test: spdSpecial timeline (fail flee)

Draw the last card with Speed highest, opponentPower >= power, and speed <= opponentSpd:
- Step 0: special-trigger (fail flee banner), lane=2
- No participant-effect (nothing happens)
- Last step: shuffle-back

### 10. Test: shuffle-back step content

Directly test `build_shuffle_step()`:
- Create a participant with: 2 untrusted face-up board cards, 1 trusted board card, 1 face-down board card, 3 hand cards
- Assert shuffle-back step has 5 affected cards (2 untrusted + 3 hand)
- Assert trusted card is NOT included
- Assert face-down board card is NOT included
- Assert all affected have destinationLane=-1 (deck)

### 11. Test: multiple specials priority

When multiple stats tie or dominate in priority order (int > spd > vis > res):
- Only the highest-priority special's trigger step appears first
- e.g., if int and spd tie: intSpecial runs (its trigger is first), then spdSpecial if int response allows it
- Test that the timeline reflects the actual specialActions() priority logic

### 12. Test: bot special timeline

Integration test: simulate a bot drawing the last card with specific stats:
- Use `_run_bot_turn()` to produce bot_actions and timelines
- Assert bot timeline has the expected steps
- Assert timeline steps have the bot's participantId
- Assert loading animations include the draw card AND the timeline steps

### 13. Test: no spd recursion with response flag

The current spdSpecial can return a non-empty string (`f"{participant.id} fled from {opponent.id}"`) which prevents vis/res from activating. Test that:
- When spd returns a flee response, the timeline includes the flee participant-effect
- When spd returns "" (rush down or fail), the original specialActions continues

### 14. Regression: existing tests still pass

Run the full test suite:
```bash
python3 manage.py test
```
- All existing 24+ tests must pass
- No changes to game logic (mutations, card movements, turn phases)
- `test_draw_renders_drawn_card_loading_from_deck` still validates loading class
- `test_end_turn_defers_bot_turn_to_enemy_phase_get` still validates bot timing

## Files to create/modify

- **MMM/tests.py** — Add `SpecialTimelineTests` class with all the above test methods

## Acceptance criteria

- [ ] No timeline when deck still has cards after draw
- [ ] intSpecial produces trigger (lane=1) + card-effect + shuffle-back
- [ ] resSpecial produces trigger (lane=4) + card-effect (trust glow) + shuffle-back
- [ ] visSpecial win produces trigger (lane=3) + card-effect + participant-effect (opponent defeat) + shuffle-back
- [ ] visSpecial drain produces trigger + participant-effect (self defeat) + shuffle-back (no card-effect)
- [ ] spdSpecial rush down flattens opponent special steps into the same timeline (no nesting)
- [ ] spdSpecial flee produces trigger + participant-effect (flee) + shuffle-back
- [ ] spdSpecial fail produces trigger + shuffle-back (no participant-effect)
- [ ] shuffle-back step correctly excludes trusted and face-down cards
- [ ] Bot timeline construction mirrors player timeline construction
- [ ] All existing tests pass
- [ ] Edge: empty shuffle-back (all cards trusted) produces step with zero affectedCards

## Dependencies

- server-timeline-model (the timeline construction functions being tested)
- shuffle-back-animation (build_shuffle_step function)
- enemy-specials-integration (bot timeline integration)
