Render decks larger than 32 cards as a bounded active stack plus inert 32-card indicators.

Implementation:
1. In `MMM/views.py`, extend `contextBoard()`/`newBoard()` with presentation metadata while keeping `deckCount` as the real number of cards. Partition the ordered deck stack into a rightmost active stack of at most 32 cards and inert full-block indicators: 40 cards → one inert block + eight active cards; 65 → two inert blocks + one active card; an exact 32-card deck → no inert block + 32 active cards; an exact multiple above 32 → one fewer inert block than the number of full blocks so the active stack remains 32 cards. Preserve the existing iteration/top-card semantics and verify the chosen active segment against `getNextDeckCard()` before merging.
2. Update both own and enemy markup in `MMM/jinja2/MMM/battle/viewBoard.jinja2`. Use a non-expanding `.deck-stack` wrapper, render inert `<ul>` blocks before/left of one clearly marked active deck, and keep the draw form/button only on the active deck's existing top/last item. Inert blocks must contain no form, button, card id, or action hook and should be `aria-hidden`/non-interactive.
3. In `var/www/static/cards.css`, style inert blocks as shallow visual indicators positioned to the left with slight overlap and clipped/contained so many blocks do not widen the document or create horizontal page overflow. Update `.deckHand`/`.enemyDeckHand` flex selectors for the wrapper without changing the hand's available width.
4. Update `var/www/static/loadingAnimations.js` to target the active deck explicitly for negative source lanes and shuffle/deck-origin animations. Do not let `querySelector('.deck')` select an inert block. Keep the draw button and active deck available to `cardDragDrop.js` and preserve enemy display-only behavior.
5. Add rendering tests using synthetic 32-, 40-, 64-, 65-card states for both boards. Assert real counts, active/inert sizes, one draw button, no inert forms/buttons, and the existing deck-origin animation hook; perform a browser check for clipping and the enabled/blocked draw states.

Files to create/modify:
- `MMM/views.py` — calculate active deck stack and inert-block metadata for `contextBoard()` and enemy-board context.
- `MMM/jinja2/MMM/battle/viewBoard.jinja2` — active/inert deck wrappers for player and enemy decks.
- `var/www/static/cards.css` — bounded deck-stack/inert-block layout and active-deck selectors.
- `var/www/static/loadingAnimations.js` — active-deck lookup for source and shuffle animation clones.
- `var/www/static/cardDragDrop.js` — only if the final active-deck hook changes the draw-button lookup; preserve `remainingAllowances()` and disabled behavior.
- `MMM/tests.py` — 32/40/64/65-card rendering and interaction regressions.

Acceptance criteria:
- [ ] A 40-card deck renders one inert block and an eight-card active stack.
- [ ] A 65-card deck renders two inert blocks and a one-card active stack.
- [ ] A 32-card deck renders no inert block and retains its 32-card stack; larger exact multiples retain a 32-card active stack.
- [ ] Inert blocks overlap to the left, do not widen the viewport, have no form/button/action listener, and cannot be clicked or dragged.
- [ ] Only the active deck exposes the draw button, and drawing still respects server/client turn allowances.
- [ ] Enemy decks use the same visual partition but remain display-only.
- [ ] Deck counts remain real, negative-source draw animations start from the active deck, and empty decks still show `emptyDeck`.

Dependencies:
- `done/battle-interaction/loading-animations/` — current deck-origin clone lookup and phase timing.
- `done/turn-affordances/` — blocked/disabled draw semantics that must apply only to the active deck.
- `hand-card-visibility/` should land first so shared `.deckHand` geometry is stable.
- Coordinate the wrapper changes with `large-hand-rendering/`; the recommended order is this leaf first, then the hand leaf.
