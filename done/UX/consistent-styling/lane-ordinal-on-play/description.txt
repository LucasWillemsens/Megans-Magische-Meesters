Assign every newly played card the next highest ordinal in its destination lane.

Implementation:
1. Add regression cases in `MMM/tests.py` before changing behavior: play a hand card whose hand ordinal is larger than the destination lane ordinals, play into an empty lane, include an existing trusted card where relevant, and exercise the bot/special path. The expected result is destination `max(laneOrdinal) + 1`, never a value derived from the card's old hand ordinal.
2. Correct `BattleParticipant.playCard()` in `MMM/models.py`. The current code calls `state.play(lane)` at lines ~318–319 and only then calls `getNewMaxCardInLaneOrdinal(lane)`; because the moved card is already in the queryset, its stale hand ordinal can inflate the result. Compute the destination ordinal before moving the card or explicitly exclude the moving `GameCard`, then call `updateOrdinal()` after `state.play()`.
3. Define the scope of “lane maximum” as all cards currently in that lane, including trusted cards; trusted cards render in a separate row but still occupy lane ordinal space. Keep the ordinal unique and positive for an empty lane (`1`). Do not use the animation source ordinal (`sourceLane`/`sourceOrdinal`) as the destination ordinal.
4. Audit every direct lane transition found by the current code search: normal/cookie plays and special actions call `playCard()`; bot plays call `playCard()`; `startWithDeckInRandomOrder()` directly plays the trusted starting card and must initialize it to ordinal `1`; `drawCard()` and `shuffleBack()` are hand/deck transitions and must retain their existing ordinal rules. Document any intentionally non-play transition in the implementation change.
5. Re-run lane-stack rendering and loading-animation checks. Verify `contextBoard()` preserves the corrected ordinals and that higher ordinals still receive the higher visual stack order.

Files to create/modify:
- `MMM/models.py` — calculate the destination max before/without including the card being moved; initialize the direct starting-card play consistently.
- `MMM/views.py` — only if an action path bypasses `playCard()` or needs a corrected destination/source distinction.
- `MMM/tests.py` — human, bot/special, empty-lane, occupied-lane, trusted-lane, and stale-hand-ordinal regressions.
- `var/www/static/cards.css` — no change expected; inspect only to verify ordinal-based z-index behavior.

Acceptance criteria:
- [ ] A card played into an occupied lane receives exactly the existing lane maximum plus one.
- [ ] A card played into an empty lane receives ordinal `1`.
- [ ] The result is independent of the card's old hand ordinal and animation source ordinal.
- [ ] Human cookie plays, bot plays, and special-action plays use the same rule.
- [ ] The direct starting-card initialization has a deliberate valid ordinal, and no other direct play path leaves a stale ordinal.
- [ ] The corrected card renders above lower-ordinal cards and all related tests pass.

Dependencies:
- `done/core-data-model/card-state-machine/` — lane/state transition contract.
- `done/battle-page-polish/lane-card-stacking/` — ordinal-driven visual stacking that exposes stale ordinals.
- Complete before validating `large-deck-rendering/` and `large-hand-rendering/` as a final battle-layout batch, although it does not require their CSS changes.
