Repair challenge-form radio markup and preselect the first valid player, deck, and starting card.

Implementation:
1. In `MMM/jinja2/MMM/createGame.jinja2`, remove every invalid `</input>` closing tag. `<input>` is a void element; render each radio as a sibling of its `<label>` (or use a valid wrapper with the label associated by `for`), including the player, deck, “New”, and card groups.
2. Add `checked` to the first option in the `challengePlayer`, existing `startingDeck`, and `startingCard` loops with `{% if loop.first %}checked{% endif %}`. When no saved decks exist, mark the sole `startingDeckNew` option checked. Keep `required`, ids, names, values, CSRF, and the existing POST field names unchanged.
3. Preserve the completed static-card markup inside the starting-card label and the artwork/description markup for player/deck options. Ensure labels remain large enough to click the preview, and preserve visible focus for keyboard/radio navigation.
4. Add a rendered-form test that checks valid radio/label associations, exactly one checked option per available group, and default submission through `initializeGame`; cover both an existing-deck fixture and the “New” fallback. Use a browser/HTML parser check to ensure no input closing tags remain.

Files to create/modify:
- `MMM/jinja2/MMM/createGame.jinja2` — valid radio/label markup and first-option defaults for all three groups.
- `var/www/static/style.css` and/or `var/www/static/cards.css` — only if the valid sibling structure needs a focused layout/focus adjustment.
- `MMM/tests.py` — form rendering, default selection, label-click/association, and submission regressions.
- `MMM/views.py` — no change expected; only adjust if tests reveal the view supplies an invalid/empty option set.

Acceptance criteria:
- [ ] Clicking a player, deck, “New”, or card preview selects its associated radio input.
- [ ] A form with available options can submit without a manual selection because exactly the first option in each group is checked.
- [ ] Selecting another option unchecks the default and posts the replacement value.
- [ ] No `</input>` closing tag remains in `createGame.jinja2`.
- [ ] The player, saved-deck/“New”, and starting-card groups use the same valid structure and remain keyboard accessible.
- [ ] Static-card previews and existing challenge initialization behavior do not regress.

Dependencies:
- `done/web-flow/challenge-setup/` — current form fields and `initializeGame()` POST contract.
- `done/client-side-asset-generation/` — static-card label markup and CSS baseline.
- Independent of the battle leaves and of `owned-cards-sorting/`; land separately to avoid mixing unrelated template changes.
