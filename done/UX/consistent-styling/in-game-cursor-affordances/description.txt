Give board controls truthful pointer and hover feedback for flip and draw actions.

Implementation:
1. Verify the current hooks in `var/www/static/cardDragDrop.js`: player lane face-down cards receive a one-shot click listener only when they are not `.blocked`, hologram face-down cards use the same flip handler, and enemy-board content is display-only. Treat those selectors and the completed turn-affordance classes as the behavior contract.
2. In `var/www/static/cardDragDrop.css`, add `cursor: pointer` for non-blocked player-board face-down card containers and for face-down hologram card containers. Make the selector specific enough that `.blocked` cards retain `cursor: not-allowed` and enemy-board cards retain the existing default cursor.
3. In `var/www/static/cards.css`, add a small hover rotation/transition to the drawable top card/button only when the draw button is not disabled and its deck is not blocked. Keep the existing pointer cursor and do not rotate an empty deck; the later large-deck leaf must retain a stable active draw-button selector.
4. Check keyboard focus and `:hover` behavior in a normal turn, after draws are exhausted, after staged plays reduce the draw budget, on an empty deck, and in the enemy phase. Add a small stylesheet/template regression assertion only if it can be made stable without coupling tests to a particular angle.

Files to create/modify:
- `var/www/static/cardDragDrop.css` — player face-down and hologram pointer selectors.
- `var/www/static/cards.css` — conditional drawable-deck hover rotation and blocked/disabled overrides.
- `MMM/tests.py` — optional static/template assertions for the cursor hooks; do not add server behavior for a CSS-only change.

Acceptance criteria:
- [ ] A non-blocked player lane face-down card shows a pointer cursor and remains clickable for flipping.
- [ ] A non-blocked face-down hologram shows a pointer cursor and remains handled by the existing flip listener.
- [ ] Blocked face-down cards keep `cursor: not-allowed` and cannot acquire the pointer rule.
- [ ] Enemy deck, hand, and lane cards never acquire a pointer cursor.
- [ ] The drawable top deck card tilts on hover only when its draw button is enabled and the deck is non-empty.
- [ ] Disabled/blocked and empty decks do not tilt, and no drag/drop or click-to-flip behavior changes.

Dependencies:
- `done/battle-interaction/` — current click-to-flip and hologram selectors.
- `done/turn-affordances/` — `.blocked`, disabled draw buttons, and budget tooltips.
- `done/battle-page-polish/lane-card-stacking/hologram-stacking/` — current hologram-row layout (historical done snapshot; preserve its hooks).
