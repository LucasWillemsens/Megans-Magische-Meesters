Make battle, player, and challenge-page presentation consistent without changing game rules.

Implementation:
1. Use the completed `done/client-side-asset-generation/` work as the static-card baseline. Do not recreate a separate static-art task; constrain new CSS fixes to the current `.smallCard`, `.staticCard`, `.smallCardInfo`, hand, deck, and challenge-card markup.
2. Complete the battle leaves against the current board contracts: non-blocked player flip targets and drawable decks get truthful cursors/hover feedback; hand card footers and static suits remain readable; and every play path gets a correct lane ordinal.
3. Render decks as an active visible stack plus inert 32-card indicators, while preserving the real count, draw button, enemy display, and `loadingAnimations.js` source lookup.
4. Render the player's hand as stable 13-card fans inside an overflow-safe wrapper. Update animation and drag/drop selectors for multiple hand rows before keyboard selection is added.
5. Polish the player page, then add server-side collection sorting. These two leaves both touch `viewPlayer.jinja2` and `viewPlayer()` and should not be assigned concurrently.
6. Fix the challenge form's radio markup and first-option defaults. This leaf is independent of the battle and player-page leaves, but should be regression-tested through the existing create-challenge flow.
7. For every leaf, add focused Django/template tests where the behavior is server-observable and perform a browser check for layout, hover, focus, scrolling, and responsive behavior. Keep the existing `python3 manage.py test` suite green.

Subgoals and safe order:
1. `in-game-cursor-affordances/` — CSS-only pointer/hover feedback, preserving `.blocked`, disabled, empty-deck, and enemy-board behavior.
2. `hand-card-visibility/` — remove the later conflicting 7px/negative-margin rule and correct hand/static-card vertical layout.
3. `lane-ordinal-on-play/` — verify and, if necessary, fix the model paths before relying on ordinal-based stacking; cover human, bot, special, empty-lane, and occupied-lane cases.
4. `large-deck-rendering/` — add active/inert deck presentation and update selectors used by draw and flight animations.
5. `large-hand-rendering/` — add 13-card fans, scrolling, responsive sizing, and multi-fan animation/drag integration.
6. `player-page-polish/` — fix possessives, challenge-card layout/semantics, and whole-card navigation.
7. `owned-cards-sorting/` — add stable name/type/acquisition-date controls after player-page markup is settled.
8. `challenge-page-default-selection/` — repair all three radio groups and check their first valid option.

The first five leaves are a recommended serial integration order because they share battle CSS/template/animation surfaces. The last three are a second serial track because the first two share the player page; the challenge leaf may be developed in parallel but should land separately from other template changes.

Files to create/modify:
- `MMM/jinja2/MMM/battle/viewBoard.jinja2` — deck active/inert markup, hand-fan markup, and existing card attributes.
- `MMM/views.py` — board presentation metadata/hand fans and optional player-page sorting context.
- `MMM/models.py` — lane ordinal calculation only if verification finds a stale path.
- `MMM/tests.py` — ordinal, deck/hand rendering, challenge markup, and player sorting regressions.
- `var/www/static/cards.css` — card footer/hand layout, deck indicator layout, deck hover, and fan styles.
- `var/www/static/cardDragDrop.css` and `var/www/static/cardDragDrop.js` — battle cursors and selector-safe interaction integration.
- `var/www/static/laneStacking.js` and `var/www/static/loadingAnimations.js` — only where multi-row/fan or active-deck layouts require animation lookup changes.
- `MMM/jinja2/MMM/viewPlayer.jinja2`, `MMM/jinja2/MMM/createGame.jinja2`, and `var/www/static/style.css` — player/challenge presentation and valid radio/card markup.

Acceptance criteria:
- [ ] All eight child leaves have been implemented or explicitly verified as no-code-needed, with the verification recorded by the implementation agent.
- [ ] Clickable, blocked, enemy, empty, and disabled board elements remain distinguishable by cursor and focus behavior.
- [ ] Static and image cards remain readable in hand, lane, deck, player, and challenge contexts.
- [ ] Decks and hands remain usable at 32/40/65-card deck sizes, 13/14/26/27+ hand sizes, and narrow viewports without changing game counts or action semantics.
- [ ] Player-page challenges and owned cards remain navigable and legible, and the challenge form can be submitted with its documented defaults.
- [ ] `python3 manage.py test` passes after every integration step; browser checks cover both normal and blocked-turn states.

Dependencies:
- `done/battle-interaction/`, `done/turn-affordances/`, and `done/battle-page-polish/lane-card-stacking/` provide the current board DOM and interaction contracts.
- `done/client-side-asset-generation/` provides the current static-card classes and footer CSS.
- Child dependencies are listed in each leaf; in particular, `large-hand-rendering` precedes `shortcut-keys/keyboard-card-selection`, and `player-page-polish` precedes `owned-cards-sorting`.
