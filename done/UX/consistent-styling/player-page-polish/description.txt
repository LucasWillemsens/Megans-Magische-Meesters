Polish player-page possessives, challenge-card layout, and whole-card navigation.

Implementation:
1. In `MMM/jinja2/MMM/viewPlayer.jinja2`, render a grammatical possessive without changing the stored player name: names ending in `s`/`S` use `Lucas' page`, while other names use `Meg's page`. Keep profile-picture alt text separately correct and HTML-escaped.
2. Replace the current challenge-card structure (an inner title-only `<a>`, `.smallCardInfo`, and a `<ul>` containing a direct `<p>`) with valid semantic markup: one block-level link for the whole challenge card, a title/subtitle region with explicit spacing, and a valid participant list containing only `<li>` children. Remove the nested title link so there is no nested interactive anchor.
3. Give the challenge item/link stable classes such as `.challenge-card` and `.challenge-card-link` and update `var/www/static/style.css` (or the narrowest existing stylesheet) for a stacked title, loot-pile/start-time metadata, participant list, hover state, and visible keyboard focus. Preserve `draggable="false"` and the existing game URL shape `{{returnURL}}game/{{challenge.game.get().id}}/{{player.id}}`.
4. Keep challenges, loot pile, start time, challenger/opponent names, empty-state text, and the Create challenge control unchanged in meaning. Add a template/client regression test for a challenge card and browser checks for click-anywhere and keyboard activation.

Files to create/modify:
- `MMM/jinja2/MMM/viewPlayer.jinja2` — possessive conditional, valid challenge-card/link structure, and existing challenge content.
- `var/www/static/style.css` and/or `var/www/static/cards.css` — non-overlapping challenge-card layout and focus/hover styles.
- `MMM/views.py` — only if computing a possessive or stable challenge view model server-side is safer than a Jinja conditional.
- `MMM/tests.py` — Lucas/Meg possessive and whole-card/metadata rendering regressions.

Acceptance criteria:
- [ ] A player named `Lucas` renders `Lucas' page`; a player named `Meg` renders `Meg's page`.
- [ ] Challenge title, loot-pile subtitle, and start time never overlap at normal or narrow widths.
- [ ] Clicking any non-empty area of a challenge card opens the existing game/player URL.
- [ ] The whole card is keyboard-focusable/activatable, has a visible focus state, and contains no nested anchor.
- [ ] Participant names, “versus”, loot pile, start time, and empty/no-participant states remain legible and semantically valid.
- [ ] The rest of the player page and owned-card links do not regress.

Dependencies:
- `done/web-flow/landing-and-player-pages/` — existing player/challenge data and URL contract.
- `done/web-flow/challenge-setup/` — challenge content and game initialization assumptions.
- Must land before `owned-cards-sorting/` because both change `MMM/jinja2/MMM/viewPlayer.jinja2` and may adjust the same page context/styles.
