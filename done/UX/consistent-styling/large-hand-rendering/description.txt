Render the player's hand as readable rotating 13-card fans with responsive horizontal scrolling.

Implementation:
1. In `MMM/views.py`, preserve the existing ordered `handCards` list and expose a presentation-only `handFans` list of chunks of 13 cards (with one empty fan when the hand is empty). Keep `data-source-lane` and `data-source-ordinal` on every card; do not renumber game state merely to render a fan.
2. In `MMM/jinja2/MMM/battle/viewBoard.jinja2`, render the own hand inside a `.hand-scroll` wrapper with one `<ul class="hand hand-fan">` per chunk and a stable `data-fan-index`. Keep the existing hand form, hidden `card_id`, `draggable`, `.blocked`, `.ghost`, and `emptyHand` behavior. The enemy hand can remain display-only, but selectors must not accidentally treat it as the player's hand.
3. In `var/www/static/cards.css`, reset the existing 1–26 single-fan assumptions: apply the existing 13-card rotation variables independently within each `.hand-fan`, give each fan a predictable non-overlapping horizontal footprint, and make `.hand-scroll` `overflow-x: auto` with a constrained width/no page-wide overflow. Ensure a second/third fan is visible by scrolling rather than being stacked on the same coordinates.
4. Update `var/www/static/cardDragDrop.js` selectors and initialization assumptions to find all descendant `ul.hand li.cardContainer` elements. Dragging, ghosting, staged budget blocking, and keyboard eligibility must work in DOM order across fans.
5. Update `var/www/static/loadingAnimations.js`: any source lookup for `lane === 0` must choose the correct hand fan from `sourceOrdinal` (or a stable card/fan data attribute), not the first `.hand`; any player deck lookup must remain independent. Handle a source fan that becomes empty after a play with a safe last-fan/fallback position and do not leave temporary clones in the wrong fan.
6. Test at 13, 14–26, and 27+ cards, at a narrow viewport where even one fan overflows, after resize, during drag-to-lane, and during draw/play loading animations. Check that `laneStacking.js` continues to handle only lane/hologram rows and that the later shortcut leaf can enumerate all fans deterministically.

Files to create/modify:
- `MMM/views.py` — chunk own hand cards into `handFans` without changing state ordinals.
- `MMM/jinja2/MMM/battle/viewBoard.jinja2` — `.hand-scroll` and repeated `.hand-fan` markup; preserve action hooks.
- `var/www/static/cards.css` — per-fan rotation, widths, spacing, and overflow scrolling.
- `var/www/static/cardDragDrop.js` — all-fan drag/drop and allowance selectors.
- `var/www/static/loadingAnimations.js` — fan-aware hand source lookup and clone positioning.
- `MMM/tests.py` — rendered fan/empty-hand contracts and regression checks for action/animation metadata.

Acceptance criteria:
- [ ] 13 cards render one correctly rotated fan with current visual behavior.
- [ ] Cards 14–26 render a second rotated fan; card 27 starts a third, and every fan repeats the 13-card rotation pattern.
- [ ] `.hand-scroll` supplies horizontal scrolling when fans or the viewport overflow, without widening the page or causing illegible overlap.
- [ ] Dragging, ghost cards, staged `.blocked` states, and hidden/action form inputs work from every fan.
- [ ] Draw/play loading clones use the correct hand fan/source position and do not break on a fan that becomes empty.
- [ ] Empty hands retain one visible `emptyHand` placeholder and enemy hands remain non-interactive.
- [ ] The final hand DOM is stable enough for `shortcut-keys/keyboard-card-selection/` to index eligible cards.

Dependencies:
- `hand-card-visibility/` — corrected card dimensions/footer spacing.
- `large-deck-rendering/` — recommended first because both leaves adjust `.deckHand` layout and `viewBoard.jinja2`.
- `done/battle-interaction/` and `done/turn-affordances/` — existing drag/drop, cookie, and blocked-action contracts.
- Must complete before `roadmap/UX/shortcut-keys/keyboard-card-selection/`.
