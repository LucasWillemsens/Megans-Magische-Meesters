End the player's turn only after the **e** key is held continuously for one second.

Implementation:
1. Add a shared hold-action helper/state in `var/www/static/cardDragDrop.js` (or an explicitly loaded keyboard module) with one timer per action, a `completed`/`submitted` guard, and cleanup on keyup, window blur, and hidden-tab visibility changes. Ignore `event.repeat` so repeated keydown events cannot stack timers.
2. On an initial `keydown` for **e**, verify the board is in a player-action phase, no `.loading` card/timeline/phase transition is active, focus is not in an input/textarea/select/contenteditable control, and the `.end-turn` button/form is present. Treat `#turnPhase[data-phase="enemy"]` as disabled even if the button is still in the DOM.
3. Add a visible one-second progress state to `.end-turn` (for example `.shortcut-hold-loading` plus `aria-busy="true"`) immediately on keydown. Releasing **e** before the timeout must clear the timer, remove the class/ARIA state, and do nothing to the form or cookies.
4. After exactly 1000ms of continuous hold, keep the submit guard set and submit the existing end-turn form through its button/request-submit path so CSRF, action value, staged cookies, and the deferred enemy-phase cookie behave exactly as a mouse click. Do not call `_run_bot_turn()` or create a second action endpoint.
5. On completion/early release, repeated keydown, phase changes, or blur, ensure at most one submission and no stale timer/spinner. Add shared CSS animation with a reduced-motion fallback; `hold-to-draw/` must reuse it rather than define a competing spinner.
6. Verify normal click/Enter behavior, disabled enemy phase, staged plays, loading/timeline render, early release, exact one-second completion, repeated presses, tab switching, and focus in each text-control type.

Files to create/modify:
- `var/www/static/cardDragDrop.js` — e-hold state, guards, timer, cleanup, and form submission.
- `var/www/static/cardDragDrop.css` or `var/www/static/cards.css` — shared hold-progress indicator and reduced-motion style.
- `MMM/jinja2/MMM/battle/viewBoard.jinja2` — only if an explicit form/button hook or `aria` target is needed; preserve `.end-turn`, hidden action input, and `actionUrl`.
- `MMM/tests.py` — server-side unchanged end-turn/deferred-phase regression; browser test for timing and duplicate-submit behavior.

Acceptance criteria:
- [ ] Holding **e** continuously for one second submits the existing end-turn form once.
- [ ] Releasing before one second performs no submit and leaves no spinner/ARIA residue.
- [ ] Progress feedback is visible while holding and has a reduced-motion-safe fallback.
- [ ] Repeated keydown/repeat events, blur, and visibility changes cannot double-submit or leave a timer running.
- [ ] The shortcut is inactive in enemy phase, loading/timeline phases, and text controls.
- [ ] Existing staged cookies, round/deferred enemy flow, and mouse/Enter submission remain unchanged.

Dependencies:
- `done/battle-interaction/board-rendering/` — `.end-turn` form/button and hidden action field.
- `done/battle-page-polish/defer-enemy-turn-calculation/` — enemy-phase guard and deferred-turn semantics.
- `keyboard-card-selection/` and `confirm-and-navigate/` — recommended earlier because they establish shared keyboard guards in the same JS file.
- Defines the shared hold-indicator style reused by `hold-to-draw/`.
