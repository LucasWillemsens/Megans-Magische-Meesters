Draw a card only after the **d** key is held continuously for one second.

Implementation:
1. Reuse the shared hold-action helper and progress class from `hold-to-end-turn/`; do not duplicate timer/keyup/blur logic. Track the draw hold separately from the end-turn hold so unrelated keys cannot complete the wrong action.
2. On initial `keydown` for **d**, require a player-action phase, no loading/timeline transition, no text-control/contenteditable focus, a present `.deckHand button.draw`, `!button.disabled`, a non-blocked active deck, and `remainingAllowances().draws > 0`. The absent button/`emptyDeck` state is the empty-deck guard.
3. Mark the active draw button/deck with the shared visible one-second progress indicator and `aria-busy`. Early **d** release clears the timer and visual state without submitting. Ignore `event.repeat`, enemy phase, and any later budget/disabled-state change before completion.
4. After 1000ms of continuous hold, submit by clicking/request-submitting the existing draw button/form exactly as a mouse draw. Preserve CSRF, `action=draw`, staged play-cookie consumption, player draw animation, special timeline, and `nextUrl` behavior; do not POST a new shortcut action.
5. Clean up on keyup, blur, visibility change, navigation, and completion. Ensure a draw hold cannot also trigger end-turn, cannot double-submit, and cannot leave a progress class on a blocked/empty deck.
6. Test fresh, blocked, over-played, empty, enemy-phase, loading/timeline, narrow viewport, text-input, early-release, exact-completion, repeated-key, and simultaneous e/d scenarios in a real browser.

Files to create/modify:
- `var/www/static/cardDragDrop.js` — d-hold guard, timer registration, active-deck/button lookup, and existing-form submission.
- `var/www/static/cardDragDrop.css` or `var/www/static/cards.css` — reuse the hold indicator from `hold-to-end-turn/`; no second visual contract.
- `MMM/jinja2/MMM/battle/viewBoard.jinja2` — only if the final active-deck wrapper needs a stable selector; preserve `.deckHand button.draw`, disabled/title hooks, and hidden action input.
- `MMM/tests.py` — unchanged draw/deferred-animation regression; browser test for hold timing and blocked-state guards.

Acceptance criteria:
- [ ] Holding **d** for one second submits the existing draw form once.
- [ ] Early release, repeat keydown, blur, visibility change, or a budget/phase becoming invalid before completion results in no draw and no leftover indicator.
- [ ] Progress feedback is visible on the active deck/button while holding and does not appear on inert or empty deck blocks.
- [ ] The shortcut is disabled for exhausted/blocked/empty decks, enemy phase, loading/timeline phases, and text controls.
- [ ] The normal draw button, cookie staging, draw animation, special timeline, and server enforcement remain unchanged.

Dependencies:
- `hold-to-end-turn/` — shared hold timer/indicator and cleanup contract.
- `done/turn-affordances/` — `remainingAllowances().draws`, disabled deck, and blocked title behavior.
- `large-deck-rendering/` — active-deck selector must be final so inert blocks can never draw.
- `done/battle-interaction/` and `done/battle-page-polish/implement-draw-card-animation/` — existing draw form and animation pipeline.
