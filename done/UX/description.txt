Implement the remaining UX and accessibility improvements for the battle, challenge, and player pages.

Implementation:
1. Treat `consistent-styling/` and `shortcut-keys/` as the two implementation tracks. Keep this directory as the coordination plan; do not move either track to `done/` until its browser behavior and regression tests are complete.
2. Complete the consistent-styling leaves in their dependency order: battle cursors and card readability, lane ordinal correctness, large-deck rendering, large-hand rendering, then the player-page and challenge-page polish leaves.
3. Complete the shortcut-keys leaves after the final hand markup is stable: keyboard selection and preview, confirmation/navigation/cancellation, hold-to-end-turn, and hold-to-draw.
4. Preserve the existing cookie-staged action contract, turn-allowance contract, deferred enemy phase, and loading-animation pipeline. UX work must not introduce a second way to submit a draw, play, flip, or end-turn action.
5. Validate each leaf with `python3 manage.py test` plus a browser check at narrow and wide viewport sizes. Use keyboard-only checks for focus, shortcuts, blocked actions, and cancellation; use the existing `.blocked`, `#turnLimits`, `.hologramRow`, and `data-source-*` hooks as the observable contracts.

Files to create/modify:
- `roadmap/UX/consistent-styling/description.txt` — scope, child leaves, and the ordered styling plan.
- `roadmap/UX/consistent-styling/*/description.txt` — actionable implementation plans for all eight styling leaves.
- `roadmap/UX/shortcut-keys/description.txt` — shortcut flow, shared guards, and dependency order.
- `roadmap/UX/shortcut-keys/*/description.txt` — actionable implementation plans for all four shortcut leaves.
- `MMM/jinja2/MMM/battle/viewBoard.jinja2`, `MMM/views.py`, `MMM/models.py`, `MMM/tests.py` — likely implementation/test targets described by the child leaves; agents must change only the files required by their assigned leaf.
- `var/www/static/cards.css`, `var/www/static/cardDragDrop.css`, `var/www/static/cardDragDrop.js`, `var/www/static/loadingAnimations.js`, and any explicitly approved hand-layout module — battle styling, keyboard, and animation integration targets.

Acceptance criteria:
- [ ] Every UX child directory has a concrete implementation plan, file list, measurable acceptance criteria, and explicit dependencies.
- [ ] The two parent descriptions identify a single safe execution order and call out shared-file conflicts before implementation begins.
- [ ] The plan distinguishes completed foundations in `done/` from work that is still required in `roadmap/UX/`.
- [ ] The plan covers responsive rendering, pointer/keyboard accessibility, blocked-action behavior, semantic challenge-page markup, and regression verification.
- [ ] No application code, status marker, or `done/` snapshot is changed by the planning task itself.

Dependencies:
- `done/battle-interaction/` — existing board markup, drag/drop, hologram, click-to-flip, play-cookie, and loading-animation contracts.
- `done/turn-affordances/` — server/client turn budgets, `.blocked` states, tooltips, and `#turnLimits`.
- `done/battle-page-polish/lane-card-stacking/` — current stacked lane/hologram CSS and `laneStacking.js` behavior.
- `done/client-side-asset-generation/` — current static CSS-playing-card markup and footer rules; static-card art itself is not a new UX leaf.
- `roadmap/UX/consistent-styling/` must finish its large-hand layout before `roadmap/UX/shortcut-keys/keyboard-card-selection/` is implemented.
