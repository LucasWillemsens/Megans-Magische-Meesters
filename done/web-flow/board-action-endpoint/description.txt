boardAction (POST to /game/<id>/board/<player>/action/) first executes pending play cookies via playcards(), then applies action=draw (drawCard; special + shuffle when the deck empties) or action=end_turn (resetTurn, bot turns, roundNumber+1). Rule violations are caught and shown as on-page error messages. The response re-renders the board with loading animations and clears the consumed cookies.
