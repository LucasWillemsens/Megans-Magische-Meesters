Replace random card selection in special actions with deliberate tactical choices so specials feel intelligent for bots and meaningful for players.

Code locations:
- MMM/views.py — `intSpecial()`: picks which hand cards to play via `random.shuffle(handCards)` ("random for now").
- MMM/views.py — `resSpecial()`: picks which board cards to trust via `random.shuffle(newCards)` ("random for now").

Implementation notes:
- For bot participants, add a heuristic (e.g. prefer cards that maximise lane stats / enable follow-up specials) instead of shuffling.
- For human participants, surface a selection UI during their own special resolution instead of auto-picking.
- Keep `getTrustableCards()` as the source of eligible cards; only the choice among them changes.

Acceptance criteria:
- `intSpecial()` and `resSpecial()` no longer use `random.shuffle` to choose cards.
- Bot choices follow a documented heuristic; human choices come from explicit player input.
