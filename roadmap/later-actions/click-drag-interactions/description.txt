Improve click and drag interactions across hand, lanes, and board for more natural gameplay control.

Code locations:
- var/www/static/cardDragDrop.js — `setupFaceDownCardClickListeners()` (and the dragstart/dragend wiring above it): check whether a card placed in a lane is valid there, or move it to the correct lane instead of accepting any drop.
- var/www/static/cardDragDrop.js — `onCardDragStart()` / `onCardDragEnd()`: drag lifecycle hooks where lane validation and drop feedback belong.
