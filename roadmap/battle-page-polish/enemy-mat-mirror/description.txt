Mirror the enemy side of the battle mat with a flipped background image so the board reads as one continuous play surface.

Code locations:
- var/www/static/style.css — `ul.basic-mat li.enemy` currently reuses `var(--basic-mat-background)` as-is; give it a mirrored version of the player mat background.

Implementation notes:
- Either add a flipped copy of the mat image asset under var/www/static/ and reference it in a new CSS variable, or flip it in pure CSS (e.g. `transform: scaleX(-1)` on a background layer / pseudo-element) so the asset stays single-sourced.
- Keep the player side (`ul.basic-mat li` non-enemy) untouched.

Acceptance criteria:
- Enemy mat background renders mirrored relative to the player mat.
- No layout shift or stretched seams between the two halves.
