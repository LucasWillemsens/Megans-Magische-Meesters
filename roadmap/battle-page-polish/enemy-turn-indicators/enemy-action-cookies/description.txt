Record each bot action as an animation cookie when the player presses 'end turn', in the same format as player play cookies.

Implementation:
1. Modify `MMM/views.py` `_run_bot_turn()` to collect the actions each bot takes:
   - drawn cards (deck -> hand), played cards (hand -> lane), flips.
   - Record card id, source lane/ordinal and target lane for each action.
2. In `boardAction()`, after `_run_bot_turn()`, set one cookie per bot action on the response using the existing JSON payload shape: {"laneValue", "sourceLane", "sourceOrdinal", "flipFaceUp"}.
3. Namespace the bot cookies so they are never confused with player cookies by using the cookie path. The player id is used in the path. In order to distinguish players only add cookies with the correct player id in it's path:
   - Update `_addLoadingAnimations()`  parse cookies with different paths into `plays` entries tagged with the owning participant. Make sure bot players have the correct participant id.

Files to modify:
- `MMM/views.py` (_run_bot_turn ~line 506, boardAction ~line 218, playcards ~line 254, _addLoadingAnimations ~line 414)

Acceptance criteria:
- Pressing 'end turn' creates one cookie per bot action, in the same payload format as player play cookies, but with a modified path to match the bot player participant id.
- Bot cookies are never consumed by `playcards()` as player actions on the next POST.
- Bot cookies are cleared after the board renders (existing clear_cookies flow in `_render`).

Dependencies: None (first subgoal; enemy-action-animation and enemy-turn-viewing-window build on it).
